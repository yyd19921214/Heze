package com.yudy.heze.store;

import com.yudy.heze.config.ServerConfig;
import com.yudy.heze.zk.ZKClient;

public class TopicQueuePool {

    public static void startup(ZKClient zkClient,ServerConfig config) {
        return;
//        startup(null, config);
    }
}
